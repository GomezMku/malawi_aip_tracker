name: aip_tracker
description: The Malawi AIP Tracker - Building A Nation Of Self-Reliant Farmers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.1.1

  # Updated Firebase dependencies for Flutter 3.35.3
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4

  # UI & Design
  google_fonts: ^6.1.0
  intl: ^0.20.2
  
  # Utilities
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  file_picker: ^10.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/